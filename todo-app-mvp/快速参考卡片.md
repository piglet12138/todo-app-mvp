# Todo App MVP - 快速参考卡片

## 🚀 快速命令

### 本地开发
```bash
# 创建虚拟环境
python -m venv venv
source venv/bin/activate

# 安装依赖
pip install -r requirements.txt

# 运行应用
uvicorn app.main:app --reload
```

### Docker部署
```bash
# 构建镜像
docker build -t todo-app .

# 运行容器
docker run -p 8000:8000 todo-app

# 后台运行
docker run -d -p 8000:8000 --name todo-app todo-app

# 查看日志
docker logs todo-app

# 停止容器
docker stop todo-app
```

## 📁 项目结构

```
todo-app-mvp/
├── app/main.py          # FastAPI后端
├── static/              # 前端文件
│   ├── index.html
│   ├── style.css
│   └── app.js
├── requirements.txt     # Python依赖
└── Dockerfile          # Docker配置
```

## 🔄 数据流

```
浏览器 → localhost:8000 → Docker端口映射 → 容器内8000 → FastAPI
                                                      ↓
浏览器 ← HTML/CSS/JS ← /static ← StaticFiles ← FastAPI
                                                      ↓
浏览器 ← JSON数据 ← /api/todos ← API端点 ← FastAPI
```

## 🎯 关键概念

| 概念 | 说明 | 示例 |
|------|------|------|
| **镜像(Image)** | 应用的模板 | `todo-app` |
| **容器(Container)** | 镜像的运行实例 | `docker run` 创建 |
| **端口映射** | 宿主机端口 → 容器端口 | `-p 8000:8000` |
| **静态文件** | HTML/CSS/JS文件 | `/static` 路径 |
| **API端点** | 后端接口 | `/api/todos` |

## 📝 Dockerfile关键指令

```dockerfile
FROM python:3.11-slim    # 基础镜像
WORKDIR /app             # 工作目录
COPY requirements.txt .  # 复制依赖文件
RUN pip install ...      # 安装依赖
COPY app/ ./app/         # 复制代码
COPY static/ ./static/   # 复制前端
EXPOSE 8000              # 暴露端口
CMD ["uvicorn", ...]    # 启动命令
```

## 🔍 常用Docker命令

```bash
# 查看镜像
docker images

# 查看容器
docker ps -a

# 进入容器
docker exec -it <container_id> /bin/bash

# 删除容器
docker rm <container_id>

# 删除镜像
docker rmi todo-app
```

## 💡 理解要点

1. **前后端整合**
   - FastAPI同时提供静态文件和API
   - 前端通过 `fetch()` 调用后端API
   - 数据格式：JSON

2. **Docker作用**
   - 环境隔离
   - 易于部署
   - 一致性保证

3. **构建流程**
   ```
   代码 → Dockerfile → 镜像 → 容器 → 运行
   ```

4. **端口映射**
   ```
   宿主机:8000 → 容器内:8000
   ```

## 🎓 学习路径

1. ✅ **理解项目结构** - 前后端如何组织
2. ✅ **理解代码逻辑** - 前后端如何通信
3. ✅ **理解Docker** - 镜像和容器的概念
4. ✅ **理解部署** - 从代码到运行的流程
5. 🔜 **下一步** - 添加数据库、环境变量、生产部署
