# 前后端交互快速参考

## 📋 核心概念速查

### 前端三要素

| 技术 | 作用 | 文件 | 示例 |
|------|------|------|------|
| HTML | 页面结构 | index.html | `<div id="todosList"></div>` |
| CSS | 样式设计 | style.css | `.container { padding: 20px; }` |
| JavaScript | 交互逻辑 | app.js | `fetch('/api/todos')` |

### 后端核心

| 组件 | 作用 | 代码位置 |
|------|------|---------|
| FastAPI | Web框架 | `app = FastAPI()` |
| 路由 | API端点 | `@app.get("/api/todos")` |
| 模型 | 数据验证 | `class TodoItem(BaseModel)` |

### HTTP方法映射

| 操作 | HTTP方法 | 前端 | 后端 |
|------|---------|------|------|
| 获取 | GET | `fetch('/api/todos')` | `@app.get("/api/todos")` |
| 创建 | POST | `fetch('/api/todos', {method: 'POST'})` | `@app.post("/api/todos")` |
| 更新 | PUT | `fetch('/api/todos/1', {method: 'PUT'})` | `@app.put("/api/todos/{id}")` |
| 删除 | DELETE | `fetch('/api/todos/1', {method: 'DELETE'})` | `@app.delete("/api/todos/{id}")` |

## 🔄 数据流速查

### 完整请求流程

```
用户操作
   ↓
JavaScript事件
   ↓
fetch()发送请求
   ↓
HTTP请求（JSON）
   ↓
FastAPI接收
   ↓
Pydantic验证
   ↓
业务逻辑处理
   ↓
返回响应（JSON）
   ↓
前端接收响应
   ↓
更新DOM
   ↓
用户看到结果
```

### 代码对应关系

**前端发送：**
```javascript
fetch('/api/todos', {
    method: 'POST',
    headers: {'Content-Type': 'application/json'},
    body: JSON.stringify({title: "学习", description: "..."})
})
```

**后端接收：**
```python
@app.post("/api/todos")
async def create_todo(todo: TodoCreate):
    # todo.title = "学习"
    # todo.description = "..."
    return new_todo
```

## 📝 常用代码模板

### 前端：GET请求

```javascript
async function loadData() {
    const response = await fetch('/api/todos');
    const data = await response.json();
    return data;
}
```

### 前端：POST请求

```javascript
async function createData(title, description) {
    const response = await fetch('/api/todos', {
        method: 'POST',
        headers: {'Content-Type': 'application/json'},
        body: JSON.stringify({title, description})
    });
    return await response.json();
}
```

### 后端：GET端点

```python
@app.get("/api/todos")
async def get_todos():
    return todos_db
```

### 后端：POST端点

```python
@app.post("/api/todos")
async def create_todo(todo: TodoCreate):
    new_todo = TodoItem(**todo.dict())
    todos_db.append(new_todo)
    return new_todo
```

## 🎯 关键API速查

### Fetch API

```javascript
// 基本用法
const response = await fetch(url, options);
const data = await response.json();

// 选项配置
{
    method: 'POST',           // GET, POST, PUT, DELETE
    headers: {                // 请求头
        'Content-Type': 'application/json'
    },
    body: JSON.stringify(data) // 请求体
}
```

### FastAPI装饰器

```python
@app.get("/path")      # GET请求
@app.post("/path")     # POST请求
@app.put("/path/{id}") # PUT请求
@app.delete("/path/{id}") # DELETE请求
```

### Pydantic模型

```python
class Model(BaseModel):
    field: str                    # 必填字段
    optional: Optional[str] = None # 可选字段
    default: bool = False          # 默认值
```

## 🔍 调试技巧

### 前端调试

```javascript
// 1. 查看请求
console.log('发送请求:', url, data);

// 2. 查看响应
const response = await fetch(url);
console.log('响应状态:', response.status);
console.log('响应数据:', await response.json());

// 3. 错误处理
try {
    // 请求代码
} catch (error) {
    console.error('错误:', error);
}
```

### 后端调试

```python
# 1. 打印请求数据
@app.post("/api/todos")
async def create_todo(todo: TodoCreate):
    print(f"接收到的数据: {todo}")
    # 处理逻辑
    return result

# 2. 使用日志
import logging
logger = logging.getLogger(__name__)
logger.info(f"创建待办: {todo.title}")
```

## 📚 学习检查清单

- [ ] 理解HTML结构
- [ ] 理解CSS样式
- [ ] 理解JavaScript基础
- [ ] 理解DOM操作
- [ ] 理解事件处理
- [ ] 理解Fetch API
- [ ] 理解JSON格式
- [ ] 理解HTTP协议
- [ ] 理解FastAPI路由
- [ ] 理解Pydantic模型
- [ ] 理解前后端交互流程

## 🚀 下一步学习

1. **前端框架**：React / Vue.js
2. **后端扩展**：数据库集成（SQLAlchemy）
3. **认证授权**：JWT Token
4. **部署**：Docker Compose / 云服务
5. **测试**：单元测试 / 集成测试
